I built an object-oriented Todo Manager using Python classes and JSON persistence. Task objects are serialized into dictionaries before saving and reconstructed when loading.

这个项目主要运用了函数和类的知识点，让我知道了面向对象和面向过程的区别